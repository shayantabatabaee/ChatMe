<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/colorRoundedCorner" />
    <stroke
        android:width="0dip"
        android:color="@color/colorRoundedCorner" />
    <corners android:radius="@dimen/rounded_corner_radius" />
    <padding
        android:bottom="@dimen/rounded_corner_padding"
        android:left="@dimen/rounded_corner_padding"
        android:right="@dimen/rounded_corner_padding"
        android:top="@dimen/rounded_corner_padding" />
</shape>